services:
  - type: web
    name: datuxdesignweb
    env: node
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
    plan: starter
    autoDeploy: true