services:
  - type: web
    name: datuxdesignweb
    env: node
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: node ./dist/server/entry.mjs
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
    plan: starter
    autoDeploy: true